// draw GUI event is weird
// because it doesn't take into account
// the fact that I've doubled the room
// size.  So I have to give it absolute numbers.

draw_text(500,40, "escaped: " + string(escapedTotal));
